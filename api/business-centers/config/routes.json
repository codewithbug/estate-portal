{
  "routes": [
    {
      "method": "GET",
      "path": "/business-centers",
      "handler": "business-centers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/business-centers/count",
      "handler": "business-centers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/business-centers/:id",
      "handler": "business-centers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/business-centers",
      "handler": "business-centers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/business-centers/:id",
      "handler": "business-centers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/business-centers/:id",
      "handler": "business-centers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
